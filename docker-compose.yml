services:
  hotkey-editor:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    command: "moon run hotkey-editor:dx/serve-container"
    stdin_open: true
    tty: true
    environment:
      FORCE_COLOR: "1"
      RUSTUP_TOOLCHAIN: "1.95.0-x86_64-unknown-linux-gnu"
    volumes:
      - .:/app
      - cargo-registry:/root/.cargo/registry
      - cargo-git:/root/.cargo/git
      - target:/app/target
      - moon-cache:/app/.moon/cache
      - root-node-modules:/app/node_modules
      - e2e-node-modules:/app/crates/hotkey-editor/e2e/node_modules
      - moon-home:/root/.moon
    ports:
      - "8123:8123"

volumes:
  cargo-registry:
  cargo-git:
  target:
  moon-cache:
  root-node-modules:
  e2e-node-modules:
  moon-home:
