[package]
name = "tw-macro"
authors.workspace = true
edition.workspace = true
license.workspace = true
version.workspace = true
description = "Compile-time typed Tailwind class lists with a declared, keyed responsive band vocabulary."

[lib]
path = "src/lib.rs"

[features]
dioxus = ["dep:dioxus"]

[dependencies]
dioxus = { workspace = true, optional = true }
