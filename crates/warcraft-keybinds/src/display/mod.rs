//! Presentation-layer helpers that turn raw binding/database data into
//! display-ready values for the UI (tile labels, icons, inspector copy).

pub mod ability_cell;
pub mod grid_behavior;
pub mod inspector_detail;
pub mod rendered_grid;
pub mod templates;
