id: warcraft-keybinds
stack: backend
layer: library
tags:
  - rust
  - lib
language: rust

tasks:
  rust/build:
    inputs:
      - "@group(sources)"
      - "@group(configs)"
      - "templates/**/*"

  rust/test:
    inputs:
      - "@group(sources)"
      - "@group(configs)"
      - "templates/**/*"
      - "fixtures/**/*"
