id: warcraft-keybinds
stack: backend
layer: library
tags:
  - rust
  - lib
language: rust

tasks:
  rust/build:
    inputs:
      - "@group(sources)"
      - "@group(configs)"
      - "templates/**/*"
      - "/crates/hotkey-editor/templates/**/*"

  rust/test:
    inputs:
      - "@group(sources)"
      - "@group(configs)"
      - "templates/**/*"
      - "fixtures/**/*"
      - "/crates/hotkey-editor/templates/**/*"
