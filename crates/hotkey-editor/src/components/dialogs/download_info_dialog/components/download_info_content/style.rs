use dioxus::prelude::*;

/// The content block's stylesheet: the centered column that holds the
/// instructions. One sheet, no viewport bands.
pub(super) const DOWNLOAD_INFO_CONTENT_STYLES: Asset = asset!(
    "/src/components/dialogs/download_info_dialog/components/download_info_content/styles/base.css"
);
