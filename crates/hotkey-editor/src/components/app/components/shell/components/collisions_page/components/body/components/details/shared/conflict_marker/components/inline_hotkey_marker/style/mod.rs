use tw_macro::tw;
// The shared-hotkey badge shown inline between two abilities. Shared badge-wrapper VALUES with its sibling; each writes its own list.
classes! {
    base: tw![
        "self-start",
        "h-18",
        "inline-flex",
        "items-center",
        "justify-center",
    ],
}
