use tw_macro::tw;
// The shared-hotkey badge capping a multi-way stack (nudged down). Shared badge-wrapper VALUES with its sibling; each writes its own list.
classes! {
    base: tw![
        "self-start",
        "inline-flex",
        "items-center",
        "justify-center",
        "pb-2",
    ],
}
