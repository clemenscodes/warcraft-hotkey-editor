/// The preview drawer row's visible label while its preview is closed.
pub(super) const LABEL: &str = "Preview";
/// The preview drawer row's visible label while its preview is open — the row flips to this
/// so tapping it again reads as closing the preview.
pub(super) const HIDE_LABEL: &str = "Hide Preview";
