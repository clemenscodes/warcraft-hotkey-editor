//! Static labels shared by every info dialog: the required filename shown as a
//! chip and the cancel button's label.

/// The required CustomKeys.txt filename, shown as a boxed monospace chip.
pub(super) const FILENAME: &str = "CustomKeys.txt";

/// The cancel button's label.
pub(super) const CANCEL: &str = "Cancel";
