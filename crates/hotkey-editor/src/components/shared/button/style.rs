use dioxus::prelude::*;

/// The button's single stylesheet. One sheet, no viewport bands: the action
/// button is the same size at every breakpoint.
pub(super) const BUTTON_STYLES: Asset = asset!("/src/components/shared/button/styles/base.css");
